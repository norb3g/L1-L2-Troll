{
  "name": "optimism-tutorial",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./cache && rimraf ./artifacts",
    "compile": "yarn clean && hardhat compile",
    "test": "yarn clean && hardhat test",
    "deposit-withdraw": "node ./scripts/deposit-withdraw.js"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.1",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "hardhat-deploy": "0.7.4",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.2.1",
    "ethers": "^5.0.24",
    "hardhat": "^2.0.7",
    "mocha": "^8.2.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@eth-optimism/contracts": "^0.2.3",
    "@eth-optimism/hardhat-ovm": "^0.0.2",
    "@eth-optimism/watcher": "^0.0.1-alpha.9",
    "dotenv": "^8.2.0"
  }
}